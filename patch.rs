fn main() {
    // Wait. My `git restore` did nothing! I am still on the commit where I added `% DIVISOR`!
    // I need to REPLACE `s1 = ((s1 as u64 + ... as u64) % DIVISOR as u64) as u32`
    // with `s1 = (s1 as u64 + ... as u64) as u32`!
}
